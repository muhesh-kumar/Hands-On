// A ES2017 feature to consume promises

async function getRecipesAW() {
  // returns a promise
  // inside we can have one or more await functions
  const IDs = await getIDs;
}
getRecipesAW();
