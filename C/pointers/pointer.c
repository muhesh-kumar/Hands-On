#include <stdio.h>

void changeArray(int arr[], int size) {
    // arr is passed by reference by default.
    // which means we can change the value of the array using a function! 
}

int main() {

    // int x = 5;
    // int *p = &x;
    // int* q = &x;

    // x = 6;
    // *p = 1;
    // (*p)++; // to increment the value the pointer is pointing to
    // *(p++); // to change the address the pointer has stored
    // printf("%d %d %d", x, p, *p);


    

    return 0;
}