#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {

    // srand(time(NULL));
    // int randomNumber = rand();    
    // printf("%d\n", randomNumber);

    // int rand_in_range = rand() % 5 + 1; // generates random number in the range 1 to 5
    // printf("%d", rand_in_range);

    // Conditional operator example:
    5 < 1 ? printf("Yes") : printf("No");

    return 0;
}